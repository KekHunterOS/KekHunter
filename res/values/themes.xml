<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.KekHunter" parent="Theme.MaterialComponents.Light">
        <!-- customize the color palette -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="android:textColorSecondary">@color/clearText</item>
        <item name="android:textColorPrimary">@color/clearText</item>

        <item name="colorAccent">#3C3C3C</item>

        <!-- other styles/themes -->
        <item name="dialogTheme">@style/DialogStyle2</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>

        <!-- button styles -->
        <item name="materialButtonStyle">@style/kekbutton</item>
        <item name="buttonStyle">@style/kekbutton</item>

        <!-- text colors/colours -->
        <item name="android:textColorLink">#476CCD</item>
        <item name="android:textColor">@color/primaryTextColor</item>
        <item name="titleTextColor">@color/clearText</item>

        <!-- text size -->
        <item name="android:textSize">14dp</item>

        <!-- Button background colors/colours -->
        <item name="colorBackgroundFloating">@color/secondaryColor</item>

        <!-- Button text colors/colours -->
        <item name="android:negativeButtonText">#070707</item>

        <!-- Button styles-->
        <item name="buttonBarNegativeButtonStyle">@style/NegativeButtonStyle</item>
        <item name="buttonBarPositiveButtonStyle">@style/PositiveButtonStyle</item>
        <item name="buttonBarNeutralButtonStyle">@style/NeutralButtonStyle</item>

        <!-- Main styles-->
        <item name="android:windowShowWallpaper">true</item>
        <item name="android:windowBackground">@color/colorBackground</item>

    </style>

</resources>
